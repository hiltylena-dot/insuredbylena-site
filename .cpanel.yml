deployment:
  tasks:
    - export DEPLOYPATH=/home/lenahilty/public_html
    - /bin/mkdir -p /home/lenahilty/portal-auth
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/cp .htpasswd /home/lenahilty/portal-auth/.htpasswd
    - /bin/cp -R * $DEPLOYPATH
    - /bin/cp .htaccess $DEPLOYPATH/.htaccess 2>/dev/null || true
