deployment:
  tasks:
    - export DEPLOYPATH=/home/insubhmy/public_html
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/cp -R * $DEPLOYPATH
    - /bin/cp .htaccess $DEPLOYPATH/.htaccess 2>/dev/null || true
